import { css } from '../stiches.config';

export const widthVariants = css({
  variants: {
    maxWidth: {
      1: {
        maxWidth: '400px',
      },
      2: {
        maxWidth: '600px',
      },
      3: {
        maxWidth: '800px',
      },
      4: {
        maxWidth: '1000px',
      },
    },

  },
});
